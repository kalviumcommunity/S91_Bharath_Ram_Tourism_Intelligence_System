info:
  name: Get Visitor By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/visitors/VISITOR_ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
