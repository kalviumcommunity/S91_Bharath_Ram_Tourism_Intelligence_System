info:
  name: Create Visitor
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/visitors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
