info:
  name: Delete Destination
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/api/destinations/DESTINATION_ID"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
